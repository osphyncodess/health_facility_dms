const arr = [1, 2, 3];

const text = "Malawi";

text.sp;

console.log(text.includes("awi"));

arr
  .filter((val) => {
    console.log(val);
    return String(val).includes(1);
  })
  .forEach((val) => {
    console.log(val);
  });
